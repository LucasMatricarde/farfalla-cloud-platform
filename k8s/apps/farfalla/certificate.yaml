apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: app-tls
  namespace: farfalla
spec:
  secretName: app-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - __APP_HOST__
